const range = (start, end = undefined, step = 1) {
	if end === undefined {
		step = 1
		end = start
		start = 0
	}
	var diff = end - start
	if diff === 0 {
		return
	}

	



	length = 
	return Array.from(Array(end - start).keys(), x => start + (x * step)	
}